# ══════════════════════════════════════════════════════════════════
#  .env.example — سیستم مدیریت باشگاه فوتسال
#
#  نحوه استفاده:
#   Windows:  copy .env.example .env   (سپس با notepad ویرایش کنید)
#   Linux:    cp .env.example .env     (سپس با nano ویرایش کنید)
#
#  ⚠️  هرگز فایل .env را به git commit نکنید
# ══════════════════════════════════════════════════════════════════


# ──────────────────────────────────────────────────────────────────
#  تنظیمات اصلی Django
# ──────────────────────────────────────────────────────────────────

# کلید امنیتی — در production حتماً یک کلید قوی و تصادفی بگذارید
# برای تولید: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=django-insecure-change-this-to-a-50-char-random-string-in-production

# حالت دیباگ
# برای تست Windows: True
# برای Docker production: False
DEBUG=True

# هاست‌های مجاز
# برای Windows local: localhost,127.0.0.1
# برای Docker:        localhost,127.0.0.1,yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# فایل تنظیمات Django
# برای Windows CMD:   futsal_config.settings.development
# برای Docker:        futsal_config.settings.production
DJANGO_SETTINGS_MODULE=futsal_config.settings.development


# ──────────────────────────────────────────────────────────────────
#  دیتابیس
#  برای Windows CMD این بخش را خالی بگذارید — از SQLite استفاده می‌شود
#  برای Docker این URL را پر کنید
# ──────────────────────────────────────────────────────────────────

# DATABASE_URL=postgres://futsal:futsal123@postgres:5432/futsal_db
#
# فرمت:  postgres://USER:PASSWORD@HOST:PORT/DBNAME
# برای Docker: postgres://futsal:futsal123@postgres:5432/futsal_db
# برای سرور خارجی: postgres://USER:PASS@IP_ADDRESS:5432/DBNAME


# ──────────────────────────────────────────────────────────────────
#  Redis
#  برای Windows CMD این بخش را خالی بگذارید — Celery غیرفعال می‌شود
#  برای Docker این URL را پر کنید
# ──────────────────────────────────────────────────────────────────

# REDIS_URL=redis://redis:6379/0
#
# برای Docker:           redis://redis:6379/0
# برای نصب محلی Redis:  redis://127.0.0.1:6379/0


# ──────────────────────────────────────────────────────────────────
#  درگاه پرداخت زرین‌پال
# ──────────────────────────────────────────────────────────────────

# شناسه مرچنت زرین‌پال (36 کاراکتر)
# برای تست: هر مقداری بگذارید
# برای production: شناسه واقعی زرین‌پال خود را بگذارید
ZARINPAL_MERCHANT_ID=test-merchant-id-for-local-testing

# حالت sandbox (تست)
# True  = درگاه آزمایشی (پول واقعی کم نمی‌شود)
# False = درگاه واقعی (فقط وقتی کلید واقعی دارید)
ZARINPAL_SANDBOX=True


# ──────────────────────────────────────────────────────────────────
#  پوشه‌های فایل
#  معمولاً نیازی به تغییر ندارد
# ──────────────────────────────────────────────────────────────────

# MEDIA_ROOT=mediafiles
# STATIC_ROOT=staticfiles


# ──────────────────────────────────────────────────────────────────
#  superuser اولیه (فقط در Docker — docker-entrypoint.sh استفاده می‌کند)
# ──────────────────────────────────────────────────────────────────

DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_PASSWORD=Admin@1234
DJANGO_SUPERUSER_EMAIL=admin@futsal.local


# ──────────────────────────────────────────────────────────────────
#  ردیابی خطا (Sentry) — اختیاری
# ──────────────────────────────────────────────────────────────────

# اگر ندارید خالی بگذارید
# SENTRY_DSN=https://xxxx@sentry.io/xxxx
